/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/ecommerce-project",
  // output: "export",
  reactStrictMode: false,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
