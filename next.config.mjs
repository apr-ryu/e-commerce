/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/e-commerce",
  output: "export",
  reactStrictMode: false,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
